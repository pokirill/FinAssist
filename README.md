# FinAssist — iOS приложение для финансового планирования

## 📱 О проекте

**FinAssist** — это iOS-приложение для управления личными финансами и достижения финансовых целей. Приложение помогает пользователям:

- Ставить финансовые цели (квартира, путешествия, подушка безопасности)
- Планировать доходы (зарплата, премии) и расходы
- Получать точный прогноз достижения каждой цели **по дням**
- Распределять свободные деньги по приоритетам
- Понимать, когда можно покупать "хотелки" без ущерба для плана

**Техстек:**
- SwiftUI (iOS 15+)
- UserDefaults для хранения данных
- Архитектура: MVVM-подобная структура
- Язык: Swift 5.5+

---

## 🏗 Архитектура

### Основные компоненты

```
FinAssist/
├── Models/
│   ├── Goal.swift              # Модель финансовой цели
│   ├── Income.swift            # Модель доходов
│   ├── Expense.swift           # Модель расходов
│   ├── Credit.swift            # Модель кредитов
│   ├── Wishlist.swift          # Модель "хотелок"
│   └── Event.swift             # События для прогноза
│
├── Views/
│   ├── MainAppView.swift       # Главный экран "Мои цели"
│   ├── AddGoalView.swift       # Создание новой цели
│   ├── EditGoalView.swift      # Редактирование цели
│   ├── DepositView.swift       # Пополнение цели
│   ├── IncomeModalView.swift   # Управление доходами
│   ├── ExpenseModalView.swift  # Управление расходами
│   ├── SettingsView.swift      # Настройки (подушка безопасности)
│   ├── OnboardingView.swift    # Онбординг
│   └── FeedbackView.swift      # Обратная связь
│
├── Utils/
│   ├── ForecastEngine.swift    # Прогнозирование по дням
│   ├── EventBuilder.swift      # Генерация событий
│   ├── AppColors.swift         # Цветовая схема
│   └── AppUtils.swift          # Утилиты (форматирование)
│
└── FinAssistApp.swift          # Точка входа
```

---

## 📊 Основные сущности

### 1. Goal (Цель)

```swift
struct Goal: Identifiable, Codable {
    let id: UUID
    var name: String
    var targetAmount: Double        // Целевая сумма
    var currentAmount: Double       // Накоплено
    var targetDate: Date            // Желаемая дата
    var description: String
    var priority: GoalPriority      // critical / important / niceToHave
    var type: GoalType              // regular / emergencyFund
    var skipInPeriod: Bool          // Не откладывать в этот период
    
    // Расчетные поля (заполняются ForecastEngine)
    var forecastDate: Date?         // Прогнозная дата достижения
    var requiredPerMonth: Double?   // Нужно откладывать ₽/мес
    var actualPerMonth: Double?     // План дает ₽/мес
}
```

**Приоритеты целей:**
- `critical` — критичная (подушка, квартира)
- `important` — важная (крупные покупки)
- `niceToHave` — желаемая (необязательные цели)

**Типы целей:**
- `regular` — обычная цель
- `emergencyFund` — подушка безопасности (создается автоматически)

### 2. Income (Доход)

```swift
struct Income: Identifiable, Codable {
    let id: UUID
    var monthlySalary: Double       // Оклад
    var advancePercent: Double      // Доля аванса (0-100%)
    var advanceDate: Int            // День выплаты аванса
    var salaryDate: Int             // День выплаты зарплаты
    var bonuses: [Bonus]            // Премии
}
```

**Премии (Bonus):**
- Разовые (once) или регулярные (monthly/quarterly/yearly)
- Сумма и дата выплаты

### 3. Expense (Расход)

```swift
struct Expense: Identifiable, Codable {
    let id: UUID
    var housingCost: Double         // Жилье
    var utilities: Double           // Коммуналка
    var groceries: Double           // Продукты
    var transportation: Double      // Транспорт
    var subscriptions: Double       // Подписки
    var entertainment: Double       // Развлечения
    var other: Double               // Прочее
}
```

### 4. Credit (Кредит)

```swift
struct Credit: Identifiable, Codable {
    let id: UUID
    var name: String
    var monthlyPayment: Double
    var paymentDate: Int            // День месяца
    var endDate: Date?              // Дата окончания (optional)
}
```

### 5. Wishlist (Хотелки)

```swift
struct Wishlist: Codable {
    var items: [WishlistItem]
}

struct WishlistItem: Identifiable, Codable {
    let id: UUID
    var name: String
    var amount: Double
    var note: String?
}
```

---

## 🔮 Прогнозирование (ForecastEngine)

### Как работает прогноз

**ForecastEngine** — ключевой компонент, который:

1. **Генерирует события по дням** (EventBuilder):
   - Доходы (аванс, зарплата, премии)
   - Расходы (по категориям)
   - Платежи по кредитам

2. **Симулирует финансовое состояние день за днем**:
   - Рассчитывает свободный кэш после обязательных платежей
   - Распределяет свободные средства по целям согласно приоритетам
   - Определяет точную дату достижения каждой цели

3. **Рассчитывает "Нужно/План дает"** для каждой цели:
   - `requiredPerMonth` = сколько нужно откладывать в месяц, чтобы успеть к targetDate
   - `actualPerMonth` = сколько реально выделяется по плану
   - Статус: "✓ Успеваем" или "⚠ Не успеваем"

### Логика распределения средств

```swift
// Шаг 1: Рассчитываем freePerMonth (свободно после всех расходов)
let freePerMonth = totalMonthlyIncome - totalMonthlyExpense

// Шаг 2: Для каждой цели рассчитываем requiredPerMonth
requiredPerMonth = (targetAmount - currentAmount) / monthsUntilTarget

// Шаг 3: Распределяем freePerMonth по целям в порядке приоритетов
// critical → important → niceToHave, внутри по ближайшей дате
for goal in sortedGoals {
    let needed = goal.requiredPerMonth
    let alloc = min(needed, remainingFreeCash)
    goal.actualPerMonth = alloc
    remainingFreeCash -= alloc
}

// Остаток идет на "хотелки"
```

**Сортировка целей:**
1. По приоритету: `critical` → `important` → `niceToHave`
2. Внутри приоритета: по ближайшей `targetDate`

---

## 💰 Подушка безопасности (Emergency Fund)

### Особенности

- **Автоматическое создание**: подушка создается как цель типа `emergencyFund` с приоритетом `critical`
- **Целевая сумма**: `среднемесячный доход × N месяцев` (N = 3/6/9/12, по умолчанию 3)
- **Настройки** (`SettingsView`):
  - Включить/выключить подушку
  - Выбрать количество месяцев (3/6/9/12)
  - Опция "В этот период не откладывать в подушку"

### Логика работы

```swift
// MainAppView.updateEmergencyFund()
func updateEmergencyFund() {
    if emergencyFundEnabled {
        let targetAmount = totalMonthlyIncome * Double(emergencyFundMonths)
        
        if let existing = goals.first(where: { $0.type == .emergencyFund }) {
            // Обновляем существующую
            existing.targetAmount = targetAmount
        } else {
            // Создаем новую
            let fund = Goal(
                name: "Подушка безопасности",
                targetAmount: targetAmount,
                priority: .critical,
                type: .emergencyFund
            )
            goals.append(fund)
        }
    } else {
        // Удаляем подушку
        goals.removeAll(where: { $0.type == .emergencyFund })
    }
}
```

**В UI подушки:**
- Не показывается "Желаемая дата" (она рассчитывается автоматически)
- Показывается покрытие: "💰 Покрытие: X мес. из N"
- Кнопка "Настроить подушку" открывает `SettingsView`
- Нельзя редактировать/удалить вручную (только через настройки)

---

## 🎨 UI/UX компоненты

### MainAppView (Мои цели)

**Верхний блок (Summary):**
```
Свободно на цели: X ₽/мес
Цели: N в графике, M под риском  // или "пока нет целей" / "все достигнуты"
На хотелки: Y ₽/мес             // или "Всё уходит на обязательные платежи"
```

**Карточки целей (GoalCardView):**

*Свернутое состояние (по умолчанию):*
- Название цели
- Бейдж приоритета (Критичная/Важная/Желаемая)
- Бейдж статуса: "✓ Успеваем" / "⚠ Не успеваем" / "⚙️ Расчет..."
- Прогресс-бар
- Иконка развертывания

*Развернутое состояние:*
- Все из свернутого +
- Цель: X ₽, накоплено: Y ₽
- Желаемая дата (скрыта для подушки и целей с приоритетом "Желаемая")
- **Блок "Нужно/План дает":**
  ```
  Нужно: X ₽/мес          План даёт: Y ₽/мес
  ✓ Успеваем, в графике   // или ⚠ Не успеваем, нужно +Z ₽/мес
  ```
- Прогноз достижения: ДД.ММ.ГГ
- Для подушки: покрытие + кнопка "Настроить подушку"

**Цветовая индикация:**
- Зеленая окантовка: цель достигнута
- Красная окантовка: прогноз позже желаемой даты (>7 дней просрочки)

### Пустые состояния

1. **Нет финансовых данных:**
   ```
   ⚠️ Необходимо указать доходы и расходы
   [Перейти в "Доходы и расходы"]
   ```

2. **Доходов не хватает на расходы:**
   ```
   ⚠️ Доходы не покрывают обязательные расходы
   [Перейти в "Доходы и расходы"]
   ```

3. **Нет целей:**
   ```
   🎯 Добавьте первую цель
   Начните путь к своей мечте...
   [Добавить цель]
   ```

---

## 💾 Хранение данных

### UserDefaults

Все данные хранятся локально в UserDefaults (Codable):

```swift
// Ключи хранения
"user_goals"                    // [Goal]
"user_incomes"                  // [Income]
"user_expenses"                 // [Expense]
"user_credits"                  // [Credit]
"user_wishlist"                 // Wishlist
"hasCompletedOnboarding"        // Bool

// AppStorage (для настроек)
"emergencyFundEnabled"          // Bool
"emergencyFundMonths"           // Int (3/6/9/12)
"emergencyFundSkipPeriod"       // Bool
```

### Функции работы с данными

```swift
// MainAppView
func saveGoals() {
    if let encoded = try? JSONEncoder().encode(goals) {
        UserDefaults.standard.set(encoded, forKey: "user_goals")
    }
}

func loadGoals() {
    if let data = UserDefaults.standard.data(forKey: "user_goals"),
       let decoded = try? JSONDecoder().decode([Goal].self, from: data) {
        goals = decoded
    }
}

func clearAllData() {
    // Очистка всех данных для тестирования
    goals = []
    incomes = []
    expenses = []
    credits = []
    wishlist = Wishlist(items: [])
    saveGoals()
    // ... сохранение остальных данных
}
```

---

## 🚀 Запуск проекта

### Требования

- **Xcode**: 14.0+
- **iOS**: 15.0+
- **Swift**: 5.5+
- **Зависимости**: нет (только встроенные фреймворки)

### Установка

1. Клонируйте репозиторий:
   ```bash
   git clone <repository-url>
   cd FinAssist
   ```

2. Откройте проект в Xcode:
   ```bash
   open FinAssist.xcodeproj
   ```

3. Выберите симулятор или устройство

4. Запустите проект: `⌘R`

### Сброс данных для тестирования

В приложении есть функция "Очистить все данные" в меню настроек (⋯ → Очистить все данные).

---

## 🧪 Тестирование

### Ручные сценарии тестирования

#### Сценарий 1: Базовое планирование
1. Пройти онбординг
2. Добавить доход: зарплата 200,000₽ (аванс 15-го, зарплата 30-го)
3. Добавить расходы: жилье 50,000₽, продукты 30,000₽, прочее 20,000₽
4. Создать цель "Отпуск": 300,000₽, приоритет "Важная", дата через 6 месяцев
5. **Проверить:**
   - Свободно на цели: ~100,000₽/мес
   - Прогноз достижения: ~3 месяца
   - Статус: "✓ Успеваем"

#### Сценарий 2: Подушка безопасности
1. Перейти в Настройки (⋯)
2. Включить подушку на 3 месяца
3. **Проверить:**
   - Появилась цель "Подушка безопасности"
   - Приоритет: Критичная
   - Целевая сумма: 600,000₽ (200к × 3)
   - Покрытие: 0 мес. из 3
4. Развернуть карточку подушки
5. Нажать "Настроить подушку" → должны открыться настройки

#### Сценарий 3: Несколько целей с приоритетами
1. Создать 3 цели:
   - "Квартира": 5,000,000₽, Критичная, 2 года
   - "Машина": 1,500,000₽, Важная, 1 год
   - "Новый ноутбук": 150,000₽, Желаемая
2. Установить доход 300,000₽, расходы 150,000₽
3. **Проверить:**
   - Порядок распределения: Подушка → Квартира → Машина → Ноутбук
   - Некоторые цели могут быть "под риском"
   - Верхний блок показывает "X в графике, Y под риском"

#### Сценарий 4: Премии и бонусы
1. Добавить премию: 100,000₽, ежеквартальная, 10-е число
2. **Проверить:**
   - Прогноз учитывает премии
   - Даты достижения целей сдвинулись

#### Сценарий 5: Кредит с датой окончания
1. Добавить кредит: 30,000₽/мес, до 31.12.2026
2. **Проверить:**
   - Прогноз учитывает кредит до даты окончания
   - После даты окончания свободных средств становится больше

---

## 🐛 Известные ограничения

1. **Локальное хранение**: данные только на устройстве (нет iCloud sync)
2. **Один пользователь**: нет авторизации/профилей
3. **Базовая валюта**: только рубли (₽)
4. **Нет экспорта**: невозможно экспортировать данные в Excel/CSV
5. **Wishlist**: функционал хотелок реализован частично (только список)

---

## 📝 Дорожная карта (TODO)

### P1 — Критичные улучшения
- [ ] Уведомления о приближении даты цели
- [ ] Виджет для главного экрана iOS
- [ ] iCloud синхронизация данных

### P2 — Важные фичи
- [ ] Полноценный функционал Wishlist (прогноз дат покупки)
- [ ] Виртуальные вклады (конверты) с рекомендациями
- [ ] Аналитика и графики (динамика накоплений)
- [ ] Экспорт данных в CSV/Excel

### P3 — Nice to have
- [ ] Множественные валюты
- [ ] Цели типа "Путешествие" с особой логикой
- [ ] Интеграция с банковскими API
- [ ] Совместное планирование (семейный бюджет)

---

## 👥 Для разработчиков

### Структура проекта

- **Models**: чистые структуры данных (Codable)
- **Views**: SwiftUI вьюхи, каждая отвечает за свой экран
- **Utils**: вспомогательные классы (ForecastEngine, EventBuilder, AppUtils)
- **FinAssistApp**: точка входа, определяет начальный экран (онбординг/главный)

### Ключевые файлы для понимания логики

1. **ForecastEngine.swift** — сердце приложения, прогнозирование
2. **MainAppView.swift** — главный экран, интеграция всех данных
3. **Goal.swift** — модель цели со всеми полями

### Добавление новой цели

```swift
// 1. Обновить GoalType enum в Goal.swift
enum GoalType: String, Codable {
    case regular
    case emergencyFund
    case travel  // новый тип
}

// 2. Добавить логику в ForecastEngine если нужна особая обработка
// 3. Обновить UI в MainAppView/GoalCardView

// 4. Обновить AddGoalView для выбора типа
```

### Дебаг

- В `GoalCardView` есть DEBUG блок, показывающий `requiredPerMonth` и `actualPerMonth`
- Для очистки данных: меню → "Очистить все данные"
- Логи прогноза можно добавить в `ForecastEngine.dayByDayForecast`

---

## 📄 Лицензия

[Укажите лицензию проекта]

---

## 📞 Контакты

Для вопросов и предложений: [укажите контакты]

---

**FinAssist** — ваш личный финансовый помощник! 💰🎯

